"""Request and response models for the HTTP layer.

Kept separate from `models/` — which holds the domain shapes used across
extraction and storage — because these exist only to describe the API's edge.
"""
